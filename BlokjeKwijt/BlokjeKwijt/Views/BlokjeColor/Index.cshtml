@model IEnumerable<BlokjeKwijt.Domain.BlokjeColor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Kleur
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RgbValue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td style="background-color:@Html.DisplayFor(modelItem => item.RgbValue);">
   
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RgbValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> 
            </td>
        </tr>
}
    </tbody>
</table>
